{
  "name": "desafio-qa-isaac",
  "version": "1.0.0",
  "description": "Suíte de testes E2E e aplicação alvo encapsulada para o desafio de QA",
  "main": "index.js",
  "scripts": {
    "docker:run": "docker compose up --build --exit-code-from testes-cypress",
    "docker:clean": "docker compose down -v",
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run",
    "test": "npm run docker:run"
  },
  "devDependencies": {
    "cypress": "^15.14.1",
    "cypress-mochawesome-reporter": "^4.0.2",
    "typescript": "^5.0.3"
  }
}